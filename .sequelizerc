require("babel-register");

const path = require("path");

module.exports = {
  config: path.resolve("sequelize", "database", "config.js"),
  "models-path": path.resolve("sequelize", "app", "models"),
  "seeders-path": path.resolve("sequelize", "database", "seeders"),
  "migrations-path": path.resolve("sequelize", "database", "migrations"),
  debug: true,
};
